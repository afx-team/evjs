import { definePageConfig } from "@evjs/ev";

export default definePageConfig({
  title: "evjs MPA About",
  meta: {
    description: "The about Page in the canonical evjs MPA example.",
    "theme-color": "#f8fafc",
  },
});
