import { definePageConfig } from "@evjs/ev";

export default definePageConfig({
  title: "About evjs",
  meta: {
    description: "About the canonical evjs Page-and-Route model.",
    "theme-color": "#f8fafc",
  },
});
